pluginManagement {
	repositories {
		maven { url 'https://repo.spring.io/milestone' }
		maven { url 'https://repo.spring.io/snapshot' }
		maven { url 'https://repo1.maven.org/maven2' }  // Add Maven Repository for OpenCSV
		gradlePluginPortal()
	}
}
rootProject.name = 'car-theft-api'
